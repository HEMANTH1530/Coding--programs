#include <stdio.h>

int main() {
    int unsorted[5]={5,1,2,4,3};
    for(int i=1;i<5;i++)
    {
        int temp=unsorted[i];
        int j=i-1;
        while(j>=0 && unsorted[j]>temp)
        {
            unsorted[j+1]=unsorted[j];
            j--;
        }
        unsorted[j+1]=temp;
    }
    for(int i=0;i<5;i++)
    {
        printf("%d ",unsorted[i]);
    }
    
    return 0;
}
